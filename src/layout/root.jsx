import React from 'react';
import { Outlet } from 'react-router';

const root = () => {
    return (
        <div>
            <Outlet>
                
            </Outlet>
        </div>
    );
};

export default root;